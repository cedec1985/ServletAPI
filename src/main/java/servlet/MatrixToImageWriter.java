package servlet;

import com.google.zxing.common.BitMatrix;
import javax.servlet.ServletOutputStream;

class MatrixToImageWriter {

    static void writeToStream(BitMatrix matrix, String png, ServletOutputStream outputStream) {
    }
}
